name: restart-prod-api

on:
  push:
    branches:
      - AuScopePortalProd

permissions:
  id-token: write
  contents: read
  packages: write

jobs:

  restart:
    runs-on: ubuntu-latest

    steps:
      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Restart Production Container App
        uses: azure/CLI@v2
        with:
          azcliversion: latest
          inlineScript: |
            set -e

            az account set --subscription ${{ secrets.AZURE_SUBSCRIPTION_ID }}

            echo "Getting latest revision for: ${{ secrets.AZURE_PRODCONTAINERNAME }}"

            revname=$(az containerapp revision list -n ${{ secrets.AZURE_PRODCONTAINERNAME }} -g ${{ secrets.AZURE_PRODRG }} --query "sort_by(@, &properties.createdTime)[-1].name" -o tsv)

            echo "Restarting revision: $revname"

            az containerapp revision restart --revision $revname -g ${{ secrets.AZURE_PRODRG }}
